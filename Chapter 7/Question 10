def is_prime(num):
    if num > 1:
        for i in range(2, num // 2):
            if (num % i) == 0:
                print("FALSE")
                break
        else:
            print("TRUE")

    else:
        print("FALSE")
